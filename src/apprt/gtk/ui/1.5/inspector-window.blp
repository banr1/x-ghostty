using Gtk 4.0;
using Adw 1;

template $XGhosttyInspectorWindow: Adw.ApplicationWindow {
  title: _("XGhostty: Terminal Inspector");
  icon-name: "com.mitchellh.xghostty";
  default-width: 1000;
  default-height: 600;

  styles [
    "inspector",
  ]

  content: Adw.ToolbarView {
    [top]
    Adw.HeaderBar {
      title-widget: Adw.WindowTitle {
        title: bind template.title;
      };
    }

    Gtk.Box {
      orientation: vertical;
      spacing: 0;
      hexpand: true;
      vexpand: true;

      $XGhosttyDebugWarning {
        visible: bind template.debug;
      }

      $XGhosttyInspectorWidget inspector_widget {
        notify::surface => $notify_inspector_surface();
        surface: bind template.surface;
      }
    }
  };
}
