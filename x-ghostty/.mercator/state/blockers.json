{
  "schema_version": "2.0.0",
  "items": [
    {
      "id": "B-001",
      "type": "external",
      "status": "resolved",
      "title": "Autonomous verification commands cannot run under the deployment OS sandbox",
      "description": "(1) `just test` (zig build test): a fresh project-local package cache needs downloads from deps.files.ghostty.org, which is not on the sandbox network allowlist (403 via proxy). (2) `just swift-test` / xcodebuild: fails under the OS sandbox even with repo-local DerivedData/TMPDIR/result bundle (added as `just check swift`): XPC service connections (build/log/simulator services) and writes to the per-user darwin temp dir are denied, ending in 'Could not resolve package dependencies: permissionDenied'. Both are deployment-level constraints (I-029 sandbox config), not project code issues. Every Essence success gate (just build-app / just test / just swift-test) is affected.",
      "affects": [
        "T-001",
        "T-005"
      ],
      "created_at": "2026-08-07T23:05:00+09:00",
      "resolved_at": "2026-08-08T17:35:00+09:00",
      "resolution": "Human resolved R-001 with option (c): ESSENCE.md now declares `profile: unsandboxed` and the deployment was re-initialized without the OS sandbox (META.md §11.5). Verified this cycle: `just test` exit 0, `just swift-test` exit 0 (TEST SUCCEEDED, 410 tests incl. 14 GroupNoteTests), `zig build` (body of `just build-app`) exit 0."
    }
  ]
}
