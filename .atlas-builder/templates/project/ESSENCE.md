<!-- ATLAS-BUILDER-TEMPLATE-PLACEHOLDER
このファイルは人間だけが編集できます(I-004)。
Atlas Builder はこのファイルを「地形」として扱い、絶対に書き換えません。

以下の9節を実際の Essence で埋めてください。各節には未記入マーカー
`<!-- FILL: ... -->` が置かれています。書き終えたらそのマーカーを削除して
ください。**未消化の `<!-- FILL:` マーカーが 1 つでも残っている間**、
doctor / validate は「Essence 未記入」と判定し、ループは cycle 開始前に
停止します(§13.1-8)。このガイドコメント全体(このブロック)も、書き終えたら
削除して構いません。
本文中の §n / I-nnn は、制御プレーンの設計書(../.atlas-builder/META.md)の
節番号・安全不変条件を指します。

見出し契約(§13.1-15): H1 `# ESSENCE — <題名>` と、下に並ぶ 9 個の H2 は、
綴り・順序ともに固定の閉じた集合です。見出しの追加・削除・改名・並べ替えは
できません。集合外の H2 の出現・見出しの欠落・重複・順序違反はすべて
validate error となり、ループは cycle 開始前に必ず停止します(停止理由
`essence_structure`)。人間が見出しを正準の形へ直して `just resume` するまで
解除されません。各節の内部で使う H3 以下の小見出しは自由です — 好きなだけ
細分してよく、検査対象にもなりません。

書き方の4原則:
1. 意図(地形)を書く。実装方法(how)も、こだわりがあるなら意図の一部 —
   どんどん書く。こだわりのない実装詳細を設計で埋めるのは Atlas Builder の仕事
   (§2.1.3)。
2. 観測可能・検証可能な言葉で書く。「良い感じに」は投影できない。
3. ここに書かれなかった意図は、存在しないものとして扱われる。
4. 必須対応事項同士の矛盾・重大な曖昧さは Blocking となり、ループは停止する
   (§13.1)。

directive の書き方(recipe: / deps: / profile:):
`recipe:`(§29)と `deps:`(§11.2)と `profile:`(§11.5)は、コメント・
コードフェンスの外にある1行のプレーンテキストとして書けば、本文中のどの節に
あっても等しく機械可読な指示として発効します(位置に依存しません)。
置き場所に迷ったら「前提事項」節へ書くことを推奨します。

  レシピ(具体的実装の型、§29):
  recipe: agentic-state-loop@2

  人手確認なしで install を許す依存(§11.2):
  deps: npm:@biomejs/biome, npm:vitest, pypi:pytest, crates:serde, lean:mathlib

  実行環境の緩和(§11.5。値は standard / auto-approve / unsandboxed の
  3つだけ、省略時は standard)。auto-approve は依存・High-Risk 等の
  人間承認プロンプトを監査付きの自動許可に置き換えます(OS sandbox は維持。
  禁止事項の deny はどの値でも緩みません)。unsandboxed はさらに OS sandbox
  も外します(sandbox がツールチェーンを構造的に阻む場合のみ)。宣言・変更は
  `just init` の再実行で発効します(§26):
  profile: auto-approve

上の3行は説明例なので現在は無効です。このガイドコメントの中に書かれている
限り発効しません — 必要な行だけをコメントの外(推奨は前提事項節)へ
プレーンテキストとしてコピーしてください。

添付資産(essences/、任意):
Markdown 以外の指示資料や、この Essence に埋め込む画像などは
`PROJECT_ROOT/essences/` 配下に置けます。サブディレクトリで整理してよく、
`essences/` から数えて最大 3 階層まで(例 `essences/input/2024/main.csv`)です。
各資産は本文で `essences/<パス>` と正確に書いて必ず言及してください。
`essences/input/` のようにディレクトリを言及すれば、その配下すべてが覆われます
(未言及=orphan・不在=dangling はどちらもループを停止します)。essences/ も
人間専用で、Atlas Builder は参照するだけで変更しません(I-004、§2.1.5)。
-->

# ESSENCE — PROJECT_TITLE

## モチベーション

<!-- FILL: このプロジェクトが存在する理由 — 誰の・どんな問題を・なぜ今解くのか。
     すべての Spec はこの節から導出理由を引く。製品名や技術名ではなく、
     達成したい「変化」を人間の言葉で書く。 -->

## 思想

<!-- FILL: このプロジェクトを貫く設計哲学と、衝突時にどちらを選ぶかの判断基準 —
     必須対応事項同士や、品質と速度のような価値観が衝突しかけたときに、
     人間としてどちらを優先するか。そして、状況次第でどうしても機械的に
     決め切れず必ず人間に委ねたい判断があれば、それも明記する(§13.1 の 4)。
     ここが明確なほど agent は停止せず正しい側を選べる。旧「優先順位」節が
     担っていた衝突時の判断基準は、この節へ書く。 -->

## 前提事項

<!-- FILL: 技術・運用・組織上の制約と、崩れたら報告してほしい前提 — 技術スタック、実行環境、
     既存資産・現行システムなどの背景・現状、データ・秘密情報の扱い、期限など
     「作り方」への制約。実装方法へのこだわりは、強度に応じて 必須対応事項 /
     任意対応事項 / ここ のいずれかに置く(§2.1.3)。前提が崩れていた場合、
     agent は勝手に読み替えず、停止して人間に報告する(§13.1)。recipe: /
     deps: / profile: の directive 行の推奨置き場でもある。 -->

## 成功条件

<!-- FILL: 成功をどう観測するか(検証可能な受け入れ基準として) — 「〜できる」ではなく
     「コマンド X が exit 0 で終わる」「Y を開くと Z が表示される」のような観測可能な
     事実で書く。各項目は Spec へ acceptance check として trace され、必須対応事項
     完了の evidence がこの check を直接検証する(§2.1.1, §22)。曖昧だとループは完了を宣言
     できない。 -->

## 必須対応事項

<!-- FILL: 絶対に守るべき要求 — 交渉不可能。1項目 = 1要求で、それぞれ検証可能に書く。
     互いに矛盾させない(矛盾は Blocking Recommendation となり停止する)。投影された
     Todo の priority `must` に対応する(§9.1, §19.3)。実装方法への交渉不可能な
     こだわり(技術選定・設計方針・書き方の流儀)も、こだわりである限りここに
     書いてよい(§2.1.3)。 -->

## 任意対応事項

<!-- FILL: 交渉可能な要求 — 必須対応事項と衝突したら犠牲にしてよい。ここが agent の自律的
     トレードオフの余地になる。投影された Todo の priority `should` に対応し、必須対応事項
     完了後に人間がフェーズ承認したときだけ自律予算を得る(§19.3)。譲れないものは
     必須対応事項へ移す。交渉可能な実装のこだわり・好み(できればこの書き方で、など)も
     ここへ(§2.1.3)。 -->

## 非対応事項

<!-- FILL: 明確にやらないこと — 有用に見えても作らない・やらない。自律ループのスコープ拡大を
     防ぐ柵。Todo にはならず、機械化可能な非対応事項は deny ルール提案として triage される
     (§2.1.2)。理想には含まれるが現段階では実施しないと決めたことも、ここへ
     「〜(現段階では実施しない。<観測可能なトリガー>が観測されたら update-essence で
     必須対応事項/任意対応事項への昇格を検討)」の形で書いてよい — トリガーは将来の
     update-essence への申し送りであり、ループが自動で昇格させることはない(§2.1.4)。 -->

## 遂行順序

<!-- FILL: 作業をどの順で進めてほしいか — 依存関係や、レビューのしやすさのために
     こだわりたい着手順があればここに書く。ただし must フェーズ境界(§19.3)は
     上書きできない — 任意対応事項を必須対応事項より先に実行させる指示はここに
     書けない(必須対応事項が終わるまで任意対応事項は runnable にならない)。
     こだわりの無い部分は Atlas Builder の裁量に任せてよく、その場合は素直に
     「順序へのこだわりは無く、Atlas Builder の裁量に任せる」と明記する。 -->

## 用語

<!-- FILL: このプロジェクト固有のドメイン用語の定義 — 1項目 = 1用語。誤読・誤投影を
     防ぐため、面談や本文中に出てくる言葉で読み手により意味がぶれうるものは
     ここで定義を確定する。定義すべき用語が特に無ければ、節自体は必須なので
     削除せず「なし」とだけ1行書く。 -->
